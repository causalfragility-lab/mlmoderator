% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlm_probe.R
\name{mlm_probe}
\alias{mlm_probe}
\title{Probe simple slopes from a multilevel interaction}
\usage{
mlm_probe(
  model,
  pred,
  modx,
  modx.values = c("mean-sd", "quartiles", "tertiles", "custom"),
  at = NULL,
  conf.level = 0.95
)
}
\arguments{
\item{model}{An \code{lmerMod} object containing a two-way interaction between
\code{pred} and \code{modx} in the fixed-effects structure.}

\item{pred}{Character scalar. Name of the focal predictor variable.}

\item{modx}{Character scalar. Name of the moderator variable.}

\item{modx.values}{Strategy for selecting moderator values. One of:
\itemize{
\item \code{"mean-sd"} (default): mean --- 1 SD, mean, mean + 1 SD.
\item \code{"quartiles"}: 25th, 50th, 75th percentiles.
\item \code{"tertiles"}: 33rd and 67th percentiles.
\item \code{"custom"}: use values supplied via \code{at}.
}}

\item{at}{Numeric vector of custom moderator values. Used when
\code{modx.values = "custom"}, or to override any strategy.}

\item{conf.level}{Confidence level for intervals. Default \code{0.95}.}
}
\value{
An object of class \code{mlm_probe} (a list) with components:
\itemize{
\item \code{slopes}: a data frame with columns \code{modx_value}, \code{slope}, \code{se}, \code{t},
\code{df}, \code{p}, \code{ci_lower}, \code{ci_upper}.
\item \code{pred}, \code{modx}: names of the predictor and moderator.
\item \code{modx.values}: the strategy used.
\item \code{conf.level}: the confidence level.
\item \code{model}: the original model (stored for downstream use).
}
}
\description{
Computes simple slopes of a focal predictor (\code{pred}) at selected values of a
moderator (\code{modx}) from a two-level mixed-effects model fitted with
\code{\link[lme4:lmer]{lme4::lmer()}}. Returns estimates, standard errors, \emph{t}-values, \emph{p}-values,
and confidence intervals in a tidy data frame.
}
\examples{
set.seed(1)
dat <- data.frame(
  y   = rnorm(200), x = rnorm(200),
  m   = rep(rnorm(20), each = 10),
  grp = factor(rep(1:20, each = 10))
)
dat$y <- dat$y + dat$x * dat$m
mod <- lme4::lmer(y ~ x * m + (1 | grp), data = dat,
                  control = lme4::lmerControl(optimizer = "bobyqa"))
mlm_probe(mod, pred = "x", modx = "m")
mlm_probe(mod, pred = "x", modx = "m", modx.values = "quartiles")
mlm_probe(mod, pred = "x", modx = "m", modx.values = "custom", at = c(-1, 0, 1))

}
